// permenant libs
#include <assert.h>
#include <iostream>
using namespace std;

/* problem description & site: https://leetcode.com/problems/median-of-two-sorted-arrays

There are two sorted arrays nums1 and nums2 of size m and n respectively.

Find the median of the two sorted arrays. The overall run time complexity should be O(log (m+n)).

*** hard!!!


*/

// problem assigned libs
#include <vector>

// add libs

class Solution_4 {
public:

	static void test() {

	}

	double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
		int lb, rb; // left, right bound of the index of median of the "combined array"
		while (rb - lb > 1) {

		}

	}
};